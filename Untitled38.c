#include<stdio.h>
#include<conio.h>
main()
{
	char c[30];
	printf("enter a string :");
    gets(c);
    puts("enter the string of u r own");
    puts(c);
    
	
	/* getche chow the charactr
	getch not show the char
	getchar and put char realted
	getchar is similar to scanf*/
	
}

